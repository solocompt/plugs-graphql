============
Installation
============

At the command line::

    $ easy_install plugs-graphql

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-graphql
    $ pip install plugs-graphql
